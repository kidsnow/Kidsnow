#pragma once

#include <GLFW/glfw3.h>
#include <glm/vec3.hpp>

namespace kidsnow {

    class Renderer {
        public:
            Renderer();
            ~Renderer();

        public:
            void Render();

        private:

    };

}
