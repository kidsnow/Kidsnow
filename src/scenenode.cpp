#include "scenenode.h"

namespace kidsnow {

SceneNode::SceneNode()
{
	for (Children::iterator child = m_children.begin(); child != m_children.end(); ++child)
	{

	}

	for (auto child = m_children.begin(); child != m_children.end(); child++)
	{
		
	}
}



}